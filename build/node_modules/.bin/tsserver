#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../tsc/bin/tsserver" "$@"
  ret=$?
else 
  node  "$basedir/../tsc/bin/tsserver" "$@"
  ret=$?
fi
exit $ret
